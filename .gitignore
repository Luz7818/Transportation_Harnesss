# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.venv/
venv/

# 本地账号凭据(含会话签名密钥,严禁提交仓库)
webapp/auth.json
.env

# LLM 运行期产物(缓存/审计日志/AI 草稿)
# 前导斜杠限定只匹配仓库根:无斜杠的 `drafts/` 会连带吞掉
# miniprogram/pages/drafts/,导致该页面 4 个文件从未入库而 app.json 却注册了它。
/llm_cache/
/llm_runs.jsonl
/drafts/

# AI 编码助手本地工作区
/.zcode/

# 备份
/backups/

# 运行期产物
*.log
.DS_Store

# 打包产物(仅根目录 dist/;sdk/dist 的 wheel/sdist 按约定入库)
/dist/

# 本机真机部署产物(含真实令牌/二维码)
启动后端.bat
miniprogram/config.js
miniprogram/preview_qr.png
miniprogram/preview_info.json
harness_service*.log
