# 架构与设计决策

本文回答「为什么这样设计」。分层与目录见 [README.md](README.md),这里只讲取舍。

## 1. 分层:被测对象与评测框架严格解耦

```
客户端(Web / 小程序 / CLI)
        │  HTTP
        ▼
FastAPI 服务层(webapp/)          ← 鉴权、校验、编排,不含业务算法
        │  函数调用
        ▼
评测框架(harness/)               ← 只依赖「管线是 receive(segments) → return(dict) 的纯函数」
        │  调用被测函数
        ▼
被测管线(pipeline/versions.py)   ← 交通算法,可以被换成任何「输入列表 → 输出字典」的函数
```

**解耦协议极简**:被测对象只需要满足
`analyze(segments: list[dict]) -> dict`,harness 对其内部实现零感知。
换来两个能力:

- **多版本共存**:`PIPELINES` 注册表把版本名映射到函数,评测/对比/自进化都基于版本名工作,
  新增 v3 只是注册表加一行;
- **场景零成本**:雨天/事故/晚高峰全部建模在数据集参数里(容量系数/流量/车速),
  同一份 v2 管线不做任何修改即可被 `evalset_scenario_rain` 检验 —— 场景属于数据,不属于代码。

## 2. replaycase:输入、期望与判分规则三位一体

一条 case 不是一段文字描述,而是「可重放的最小评测单元」:

```jsonc
{"dataset_name": "base",                    // 重现问题需要的输入
 "checks": [{"type": "classify", "segment": "S-02", "expected": "拥堵"}],  // 怎样算修好
 "label": "阈值错误", "source": "线上点踩"}  // 聚类与溯源
```

**为什么判分规则(checks)存在 case 里而不是 judge 里**:判分标准是这个 badcase 的业务语义
的一部分 —— 同一个「拥堵判定」问题,有的 case 要求等级精确相等,有的只要求指标在容差内。
规则跟着 case 走,沉淀 case 的人(可能不是写 judge 的人)才能完整表达「什么是对的」;
judge 只负责「解释规则」,新增规则类型才不会牵一发动全身。

**为什么按失败标签分目录**:失败标签是定位迭代方向的第一手聚类信息
(「v1 之后剩下的失败全在『边界处理』标签下 → 下一轮就做边界」),物理分目录让
`ls` 和 git diff 都能直接回答「这轮修了哪类问题」。

## 3. 自进化纪律:把工程纪律写进代码,而不是写在口头

`harness/evolve.py` 把迭代纪律固化为可执行流程:

- **基线可配,版本派生**:基线默认 v0(完整回放历史轨迹);登记 v3 后传
  `baseline=v2` 即增量验证 —— 迭代版本从注册表派生,不硬编码;
- **单轮迭代上限(默认 2,`EVOLVE_MAX_ROUNDS` 可调)**:防止「再调一版」的无限递归,
  强迫每轮聚焦一类改动(v1 = 算法/公式,v2 = 边界/聚合);因上限未验证的版本
  显式进入 `pending_versions` 并给出继续验证的指引,绝不静默丢弃;
- **收益递减停止**:单轮提升 <5% 且无新通过案例即停 —— 迭代成本应花在收益最大的地方;
- **回归一票否决**:逐 case 差分,任何一条已通过案例翻红即 FAIL,本轮改动回退。

运行产物(逐轮轨迹、停止原因、剩余失败)全部归档,`reports/evolution_*.json` 就是
这段优化过程的「黑匣子记录」,看板时间线可直接回放。

## 4. 判分器:规则优先,LLM 兜底,接口可插拔

```
BaseJudge(抽象)── RuleJudge        classify / metric / no_crash / recommendations / congested_empty
              └─ HeuristicJudge   conclusion_keyword(关键词覆盖,离线确定性)
CompositeJudge                       按 spec.type 路由,无主 judge 支持时该检查判失败
```

- **确定性规则能判的绝不交给模型**:数值、等级、崩溃与否,规则判 100% 可复现、零成本;
- **文本质量留给 LLM**:结论文本是否「像人话」、建议是否可执行,用 `BaseJudge` 接口接入
  真实大模型打分;当前用关键词启发式替身保证整个闭环离线可复现(评测系统自己必须可复现,
  否则回归验证无从谈起);
- **判分器自身可测试**:`tests/test_judge.py` 对每条规则语义都有断言。

## 5. 存储与并发:评测资产是核心资产

- **一切皆 JSON 文件**:cases / evalsets / reports 全部落盘,git 可管、人工可读、拷贝目录即迁移,
  不引入数据库是有意为之 —— 数据规模(百级 case、千级报告)远够,换来零运维与天然的版本历史;
- **原子写**:先写 `.tmp` 再 `os.replace`,并发或中断不会留下半截 JSON;
- **复合操作加锁**:「沉淀 case + 加入评测集」、自进化全库读写,都在进程锁内串行化
  (FastAPI 同步端点跑在线程池,锁是必须的);自进化用非阻塞锁,重复触发直接 409;
- **路径安全**:数据集/评测集/报告 ID 直接拼路径且可达自 HTTP 入参,统一过
  `_safe_name()` 白名单校验,从源头杜绝 `../` 穿越;
- **清单一致性**:删除 case 时同步从所有评测集清单移除(同一把锁内),评测集永不悬空;
  `load_evalset_cases` 对清单引用不存在的 case 直接抛错,坏数据进不了评测。

## 6. 安全设计(公网部署的前提)

| 威胁 | 对策 |
| --- | --- |
| 口令泄露 | PBKDF2-HMAC-SHA256,salt 随机 + 12 万次迭代;历史单轮 SHA-256 哈希在登录成功时透明升级 |
| 口令暴力破解 | 同一用户名连续失败 5 次锁定 10 分钟;不存在用户名也执行等时哈希,防时间侧信道探测 |
| 会话伪造 | HMAC-SHA256 签名令牌(用户名+过期时间),密钥首次启动随机生成,HttpOnly + SameSite Cookie |
| API 令牌侧信道 | `X-API-Token` 用 `hmac.compare_digest` 比较,消除逐字节短路的时间差 |
| 路径穿越 | 所有拼路径的外部标识符过白名单正则 |
| CORS | `allow_credentials` 与通配符 Origin 互斥(浏览器规范),仅显式配置来源时开启 credentials |
| 容器逃逸放大 | Dockerfile 以非 root UID 运行,配 HEALTHCHECK |
| 默认口令 | `auth.json` 入 .gitignore;health 接口暴露 `default_credentials` 状态提醒改密 |

## 7. 前端与客户端策略

- **Web 端单文件无构建**:index.html 零外部依赖(无 CDN/框架),克隆即用、内网可跑,
  换来的是演示与教学场景的零摩擦;复杂度换不出去的部分(状态管理)用「每 Tab 独立渲染函数」
  的约定隔离;
- **小程序共用同一套 API**:机器客户端走 `X-API-Token`,浏览器走 Cookie 会话,服务端一条
  中间件同时支持两种身份;小程序不引入登录页,符合「外业快速记录」的定位。

## 8. 测试策略:评测系统自己先要有测试

「拿评测系统当卖点」的前提是它自身可信:

- 单元层:models 序列化往返 / storage 路径安全与清单一致性 / judge 全规则语义 /
  runner 崩溃捕获 / report 差分渲染 / 管线三版本的算法行为(含已知缺陷的负断言);
- 集成层:evolve 完整闭环在临时目录跑真数据,断言逐轮提升、无回归、归档齐全;
- 接口层:TestClient 走真实鉴权流程(登录 Cookie、API Token、401、限流锁定、参数校验、
  删除同步清单、穿越拦截);
- 隔离:夹具把 cases/evalsets/reports 重定向到临时目录,测试永不污染真实评测资产;
  登录限流状态逐用例清零;
- 端到端:`scripts/verify.py` 用**不变量**(种子行为冻结 + 通过集单调不减 + 数值抽查)
  而非硬编码得分,评测集增长后依然可用 —— 校验脚本本身不能成为迭代新 case 的阻力。

## 9. 文档体系:产品自文档化

| 文档 | 载体 | 读者 |
| --- | --- | --- |
| README | 仓库 | 首次接触项目的人:定位/架构图/工作流/目标人群 |
| ARCHITECTURE(本文) | 仓库 | 评审者与后续维护者:为什么这样设计 |
| docs/API.md | 仓库 | 集成方:全量接口参考 |
| `/help` 文档中心 | 运行实例(webapp/static/help.html) | 看板使用者:核心概念/工作流/接口/FAQ,无需登录 |
| `/docs` | FastAPI 自动生成 | 开发者:交互式调试 |

`/help` 与 `docs/API.md` 内容同构,前者面向使用产品的人(打开浏览器就能查),
后者面向读代码库的人(GitHub 上就能查)—— 成熟项目的标准是**使用者不必读源码就能用起来**。

## 10. LLM 智能层:接在薄弱环节上,而不是处处撒模型

LLM 只做规则判不了的三件事(七维设计详见 [docs/LLM.md](docs/LLM.md)):

| 能力 | 编排方式 | 治理硬边界 |
| --- | --- | --- |
| 坏例沉淀助手 | 反馈原文 + 数据集事实 + v2 试运行 → LLM 起草 → Schema 校验 → 人工确认 | 与手工沉淀走同一校验/落盘路径,人工确认也不能绕过 |
| 失败诊断 | 分析者 + 评审者(generator-critic)双智能体,编排为确定性代码 | case_id 防幻觉过滤;结论仅供参考,不自动改资产 |
| LLM-as-Judge | `conclusion_quality` 检查按评分细则打分 | 失败降级为"未通过+原因可见",评测永不因 LLM 中断 |

Runtime(`llm/runtime.py`)统一负责配置/JSON 输出/重试/缓存/审计;
未配置 Key 自动降级为确定性 Mock —— **评测系统的第一品质是可复现**,
所以 LLM 步骤必须有缓存(同输入同输出)、审计(每次调用可追溯)与离线路径,
自由 Agent 循环被刻意排除在评测器之外。

## 11. 扩展点

| 想扩展 | 动哪里 |
| --- | --- |
| 管线 v3 | `pipeline/versions.py` 实现 `analyze_v3` 并注册进 `PIPELINES`/`CHANGELOG` |
| 新检查类型 | `CheckSpec.type` 加语义 → 实现 `BaseJudge` 子类并注册到 `CompositeJudge` |
| 真实 LLM judge | 继承 `BaseJudge`,替换 `CompositeJudge` 成员;runner/报告零改动 |
| 新数据源 | `storage.load_dataset` 支持 CSV/DB,或 FastAPI 加 ingest 端点;管线零改动 |
| 新客户端 | 任何能发 HTTP 的端:REST 接口 + OpenAPI 文档(`/docs`)已就绪 |
| 抽成通用框架 | `harness/` 对领域零依赖(仅 import models),连同 `pipeline` 的注册表模式整体可迁移;`llm/` 同样与领域无关,工作流模板可复用到任意评测域 |
